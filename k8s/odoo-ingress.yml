apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-prod-ingress
  namespace: odoo-prod
  annotations:
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-name: "route"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-prod.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-service
            port:
              number: 80
      - path: /longpolling
        pathType: Prefix
        backend:
          service:
            name: odoo-service
            port:
              number: 8072
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-test-ingress
  namespace: odoo-test
  annotations:
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-name: "route"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-test.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-service
            port:
              number: 80
      - path: /longpolling
        pathType: Prefix
        backend:
          service:
            name: odoo-service
            port:
              number: 8072
